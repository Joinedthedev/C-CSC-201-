#include <stdio.h>

int main(){
    int i, *j;

    int arr[] = {2,4,5,6,7};

    j = arr; 

    printf("The adress of the first element in the array is: %u", "The value of the first element in the array is : %d", j[0], *j[0]);




}