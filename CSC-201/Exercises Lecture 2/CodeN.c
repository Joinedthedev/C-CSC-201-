#include <stdio.h>

int main(){
    int i;
    char sub[20];
    float grade;
    for(i=0; i<5; i++){
        scanf("%s", &sub);
        scanf("%f", &grade);
    }
    if (grade >= 60){
        printf("fird")
    }
if  (grade >= 50 && grade <=59)